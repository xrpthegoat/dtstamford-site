# Downtown Stamford Realty — dtstamford.com
User-agent: *
# /data/ is the search app's own backing store, not a crawl surface. Browser
# fetches are unaffected; this only stops crawler bulk-harvest of the raw JSON.
# The two AGGREGATE market-stats files stay crawlable: they are the published
# CC-BY open dataset (see /market-data.html) and contain no per-listing records.
# Specific rules first: modern parsers use longest-match, older ones first-match.
Allow: /data/stats.json
Allow: /data/stats-history.csv
Disallow: /data/
# Live IDX inventory: the /homes/ listing hub + per-listing pages are crawlable.
Allow: /homes/
Allow: /

# Explicitly welcome the major AI / answer-engine crawlers so the site can be
# read and cited by ChatGPT, Perplexity, Claude, Gemini, and AI Overviews.
# One shared group (RFC 9309 stacked User-agent lines): a named group REPLACES
# the * group entirely for that bot, so the /data/ rule must be repeated here
# or every explicitly-welcomed crawler would still bulk-harvest the raw JSON.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: ClaudeBot
User-agent: Claude-SearchBot
User-agent: Claude-User
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: meta-externalagent
Allow: /data/stats.json
Allow: /data/stats-history.csv
Disallow: /data/
Allow: /

# Deep AI digest (entity dossier + live market data, regenerated every ~3h):
#   https://dtstamford.com/llms.txt        (curated map)
#   https://dtstamford.com/llms-full.txt   (full digest — fetch this one)

Sitemap: https://dtstamford.com/sitemap.xml
